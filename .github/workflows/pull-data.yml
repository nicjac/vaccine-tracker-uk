name: Check and Pull PHE Data

on: [workflow_dispatch]

jobs:
  check_for_data:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: 3.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r ./data_retrieval/requirements.txt
    - name: Get and check data
      id: check_data
      continue-on-error: true
      run: |
        python data_retrieval/check_new_data.py; py_exit_code=$?
        echo "data_available=$py_exit_code"
        echo "data_available=$py_exit_code" >> $GITHUB_ENV
    - run: |
        echo "Debug!"
        echo $data_available
        echo $env.data_available
    - name: Update data if available
      if: ${{ env.data_available }} == 0
      uses: EndBug/add-and-commit@v7 
      with:
        add: 'src/data/vaccination-data.json'
        author_name: Nicolas Jaccard
        author_email: nicolas.jaccard@gmail.com
